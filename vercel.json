{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "app.py" },
    { "src": "/pay/(.*)", "dest": "app.py" },
    { "src": "/login/(.*)", "dest": "app.py" }
  ]
}
